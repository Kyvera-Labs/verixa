{
  "name": "@verixa/stellar-anchor",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:testnet": "vitest run --config vitest.testnet.config.ts",
    "anchor": "node dist/infrastructure/cli/anchor-cli.js",
    "lint": "eslint ."
  },
  "dependencies": {
    "@stellar/stellar-sdk": "^16.2.0",
    "@verixa/shared-kernel": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@vitest/coverage-v8": "^2.1.9",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
